# 催化文献检索阅读 App 开发计划

> 版本：V1.0
> 日期：2026-08-05
> 依据：PRD.md V1.1、TECHNICAL_DESIGN.md V1.2
> 目标平台：Windows，本人单机使用，本地浏览器访问
> 执行状态：M0～M11 工程实现已于 2026-08-06 完成；最终证据见 `IMPLEMENTATION_STATUS.md`

## 1. 计划目标

本计划用于把已确认的产品需求拆成可以按顺序执行、逐项验收的开发任务。开发中若本计划与 PRD.md 冲突，以 PRD.md 为准；技术实现细节若需调整，应先更新 TECHNICAL_DESIGN.md 并记录原因，不得暗中改变产品范围。

V1 完成的判定不是“页面可以打开”，而是以下结果同时成立：

1. 可通过桌面快捷方式启动 App，并自动打开默认浏览器。
2. 关闭全部 App 标签页后，本地后台按约定在短暂空闲后退出，不留下翻译或 OCR 子进程。
3. 刷新、关闭和重启后，设置、文献、译文、知识库、收藏、笔记、PDF 和 PDF 批注均不丢失。
4. WoS Starter API、OpenAlex/Crossref 和 WoS 官方导入三个通道均能工作，并能合并、去重和显示来源。
5. 普通、扫描和混合型 PDF 均可进入处理流程；原 PDF 永久保存，可阅读、搜索、高亮、划线和批注。
6. 本地翻译按需启动，不调用收费翻译服务，也不会在用户不知情时产生费用。
7. 缓存自动控制在 500 MB 硬上限内，清理缓存不会删除永久数据和原始 PDF。
8. 打包后的安装版可在一台未安装开发工具的 Windows 测试机上完成安装、启动、使用、退出和再次启动。

## 2. 开发原则与范围边界

### 2.1 必须遵守

- 单用户、单机、本地优先；不建设账号、权限、云端数据库或多端同步。
- 外部服务只使用免费服务或免费额度；遇到额度耗尽时明确提示并停止请求，不自动切换到付费。
- 不模拟学校登录，不抓取 WoS 登录页面，不绕过验证码、MFA、付费墙或访问控制。
- 用户通过浏览器手动使用学校权限；App 只接收 WoS Starter API 结果和用户主动导出的官方文件。
- 原始 PDF 是永久资料，不属于缓存；App 不自动删除、不修改、不向 PDF 写回批注。
- App 不承担 PDF 文件备份；内置备份只覆盖数据库、设置和批注元数据。
- 推荐必须基于透明规则并显示原因，不做不可解释的黑箱推荐。
- V1 不做 AI 摘要、全文问答、知识图谱、浏览器扩展、系统托盘常驻、后台邮件或系统通知。

### 2.2 开发时允许的默认处理

- 未提供个人头像时使用中性默认头像，后续可在设置中替换。
- 未配置外部 API Key 时，App 仍可启动，并可使用本地 PDF、知识库和 WoS 文件导入功能。
- 某一外部数据源失败时，不阻断其他数据源；结果页应展示来源状态和失败原因。
- PDF 元数据识别置信度不足时保留候选值，要求用户确认，不自动覆盖已确认数据。

## 3. 建议工程结构

正式创建项目时采用一个仓库管理前后端、启动器、测试和打包资源：

    /
      frontend/          React、TypeScript、Vite、PDF.js 页面
      backend/           FastAPI、SQLite、检索、PDF、OCR、翻译调度
      launcher/          Windows 启动、单实例、端口发现、浏览器打开、进程回收
      contracts/         前后端共享的 OpenAPI 产物和类型生成配置
      tests/
        fixtures/        API、WoS 导出、PDF 与数据库升级样本
        e2e/             端到端测试
        packaging/       安装包和纯净机冒烟测试
      scripts/           开发、检查、打包、迁移和诊断脚本
      packaging/         图标、快捷方式、安装和卸载配置
      docs/              ADR、测试记录、发布说明和用户说明
      PRD.md
      TECHNICAL_DESIGN.md
      DEVELOPMENT_PLAN.md

数据库和用户文件不得写入项目目录。运行数据统一放在：

    %LOCALAPPDATA%/CatalystLiterature/
      data/core.db
      data/cache.db
      library/pdfs/<sha256>/original.pdf
      models/
      backups/
      logs/

## 4. 质量门槛与预定验证命令

项目创建后，应把实际命令固定在根目录 README 中。计划采用以下统一入口；如果工具链调整，只允许改命令实现，不改变质量门槛。

| 检查项 | 预定命令 | 通过标准 |
|---|---|---|
| 后端静态检查 | python -m ruff check backend | 无错误 |
| 后端类型检查 | python -m mypy backend | 无错误；必要例外有注释和原因 |
| 后端测试 | python -m pytest backend/tests | 全部通过 |
| 前端代码检查 | npm run lint | 无错误 |
| 前端类型检查 | npm run typecheck | 无错误 |
| 前端单元测试 | npm run test | 全部通过 |
| 前端构建 | npm run build | 构建成功，无关键警告 |
| 端到端测试 | npm run test:e2e | 核心流程全部通过 |
| 安装包冒烟 | powershell -File scripts/smoke-package.ps1 | 安装、启动、退出、重启均通过 |
| 全量检查 | powershell -File scripts/verify.ps1 | 汇总结果全部通过 |

共同要求：

- 单元测试不得依赖真实网络和真实用户密钥；外部 API 使用固定响应样本和契约测试。
- 涉及数据库写入、文件落盘、进程退出、重复 PDF 和批注定位的高风险分支必须有自动化测试。
- 每个阶段必须先通过本阶段验收，再开始强依赖它的下一阶段。
- 每次数据库结构变化必须增加前向迁移和升级测试，不允许要求用户删除数据库重来。
- 日志不得记录 API Key、会话令牌、PDF 正文、学校登录信息或完整隐私数据。

## 5. 开发前需要准备的输入

这些输入不必全部在第一天提供，但必须在对应阶段开始前到位。

| 输入 | 最晚需要阶段 | 用途 | 缺失时处理 |
|---|---:|---|---|
| WoS Starter API Key | M3 | 主检索通道联调 | 使用模拟响应开发，不能完成真实联调验收 |
| OpenAlex 免费 API Key | M3 | 补充检索和摘要 | 暂时使用低频公共访问或模拟响应 |
| Crossref polite pool 联系邮箱 | M3 | DOI 元数据补充 | 仅使用模拟响应，不进行正式联调 |
| 官方 Hy-MT2-1.8B GGUF Q4_K_M 文件及校验值 | M7 | 本地中英翻译 | 先完成模型接口，不能完成性能验收 |
| 个人头像文件 | M5 | 左上角个人头像 | 使用默认头像，不阻断开发 |
| WoS 官方导出样本：Plain Text、RIS、Excel | M4 | 导入解析和回归测试 | 使用公开格式样例，真实兼容性待验证 |
| PDF 验收样本集 | M8 | 去重、解析、OCR 和阅读验收 | 开发团队构造非敏感样本，用户资料后验收 |

PDF 样本集至少包括：普通英文 PDF、普通中文 PDF、纯扫描英文、纯扫描中文、文字与扫描混合、同一文件改名、同 DOI 不同文件、无 DOI、无摘要、损坏文件、加密文件、大文件和双栏论文。

## 6. 里程碑总览

工作量按一名开发者的有效开发日估算，不含等待密钥、下载模型、用户验收和硬件故障时间。估算用于排序和控制范围，不是固定交付日期。

| 里程碑 | 内容 | 依赖 | 预计工作量 | 退出条件 |
|---|---|---|---:|---|
| M0 | 基线确认与技术预检 | 无 | 2–4 日 | 风险验证完成，样本和验收口径冻结 |
| M1 | 工程骨架、启动器与生命周期 | M0 | 5–7 日 | 快捷方式启动、浏览器访问、空闲退出通过 |
| M2 | 本地存储、迁移、备份与缓存 | M1 | 7–10 日 | 重启不丢数据，缓存上限和恢复验证通过 |
| M3 | 三通道检索基础与文献合并 | M2 | 8–12 日 | 两个在线补充源与 WoS API 联调、来源合并通过 |
| M4 | WoS 官方文件导入 | M2 | 4–6 日 | 三种格式预览、导入、去重和重复导入通过 |
| M5 | 核心界面、搜索结果与详情页 | M3、M4 | 8–12 日 | 搜索到入库的主流程可用，关键状态完整 |
| M6 | 知识库、标签、笔记、状态与导出 | M5 | 6–9 日 | 本地管理、检索、回收站和三种导出通过 |
| M7 | 本地翻译 | M2、M5 | 5–8 日 | 按需启动、术语保护、持久化和进程退出通过 |
| M8 | PDF 入库、去重、解析与 OCR | M2、M6；其中翻译接入依赖 M7 | 10–15 日 | 普通、扫描、混合 PDF 处理、重复判断和题名摘要翻译接入通过 |
| M9 | PDF 阅读器与批注 | M8 | 9–14 日 | 阅读、搜索、续读、高亮、划线和批注通过 |
| M10 | 推荐、期刊关注、保存检索与提醒 | M5、M6 | 6–9 日 | 规则可解释，打开 App 时检查更新通过 |
| M11 | 安全、性能、安装包与发布验收 | M7–M10 | 8–12 日 | 纯净机安装和全量回归通过 |

预计总工作量约 78–118 个开发日。关键路径是 M0 → M1 → M2 → M3 → M5 → M6 → M8 → M9 → M11；M4、M7 和 M10 可在前置条件满足后穿插执行，但 M8-13 必须等待 M7 完成。

## 7. 分阶段执行任务

### M0：基线确认与技术预检

目标：在正式搭建工程前验证最容易导致返工的技术点，冻结测试口径。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M0-01 | 建立需求追踪表，把 PRD 条目映射到任务和测试 | 无 | 每条必须需求至少对应一个任务和一个验收点 |
| M0-02 | 记录目标电脑的 Windows 版本、CPU、内存、GPU、可用磁盘 | 无 | 生成匿名硬件基线；不得收集无关隐私信息 |
| M0-03 | 验证 Python、SQLite FTS5、WAL 和最低 SQLite 3.51.3 可用性 | M0-02 | 小型验证程序能创建 FTS、启用 WAL 并报告版本 |
| M0-04 | 做 PDFium、PDF.js、PaddleOCR、llama.cpp 的最小兼容性实验 | M0-02 | 四项均能在目标机启动；记录安装体积、内存和冷启动时间 |
| M0-05 | 确定 Hy-MT2 模型获取方式、许可证、文件大小和 SHA-256 | M0-04 | 写入模型清单；确定随安装包提供或首次使用下载 |
| M0-06 | 收集并脱敏固定测试样本 | 无 | API、WoS 导出、PDF、迁移数据库样本进入 tests/fixtures |
| M0-07 | 建立 ADR 模板和版本策略 | 无 | 关键技术变更有编号、原因、替代方案和影响记录 |
| M0-08 | 验证打包工具能包含 Python、前端静态资源及原生库 | M0-03、M0-04 | 产生最小测试安装包，避免最后阶段才发现原生依赖问题 |

M0 验收门槛：

- 所有阻塞性依赖能在目标 Windows 电脑运行。
- 模型和 OCR 的许可证允许本地个人使用和随 App 分发；若不能分发，确定首次下载流程。
- 样本覆盖第 5 节规定的主要情况。
- 未决问题均有负责人、期限和不阻断开发的临时方案。

### M1：工程骨架、启动器与生命周期

目标：建立可持续开发的工程，并首先解决“只在 App 打开时运行”的核心约束。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M1-01 | 创建前端、后端、启动器、测试、脚本和文档目录 | M0 | 开发环境可一条命令安装和启动 |
| M1-02 | 配置格式化、静态检查、类型检查、单元测试和构建 | M1-01 | 第 4 节的基础检查命令可运行 |
| M1-03 | 建立 FastAPI 健康检查和 React 空壳页面 | M1-01 | 浏览器能显示服务版本、数据库状态和运行模式 |
| M1-04 | 实现随机空闲端口分配和仅回环地址监听 | M1-03 | 服务只监听 127.0.0.1 或 ::1，不暴露局域网端口 |
| M1-05 | 实现启动会话令牌、HttpOnly Cookie、Origin 检查和 CORS 白名单 | M1-04 | 无令牌或错误 Origin 的写请求被拒绝 |
| M1-06 | 实现 Windows 单实例启动器 | M1-04 | 连续双击快捷方式只保留一个后端，并聚焦或新开 App 页面 |
| M1-07 | 实现自动打开默认浏览器和启动失败诊断页 | M1-06 | 启动成功自动打开；端口或依赖失败时给出可读原因 |
| M1-08 | 实现标签页心跳、标签计数和空闲退出状态机 | M1-05 | 最后标签离开后先等待失联判定，再按约 2 分钟空闲退出；刷新不误退 |
| M1-09 | 实现子进程登记与 Windows Job Object 回收 | M1-08 | 强制关闭、正常退出和崩溃时不遗留翻译/OCR 子进程 |
| M1-10 | 生成桌面快捷方式和基础图标占位 | M1-07 | 双击快捷方式完成完整启动流程 |

生命周期专项测试：

1. 单标签启动、刷新、关闭、等待退出。
2. 多标签启动，关闭其中一个不退出，最后一个关闭后退出。
3. 浏览器崩溃或电脑睡眠后，后台不会永久常驻；恢复时不破坏数据库。
4. 翻译/OCR 正在处理时关闭所有标签，先安全取消或收尾，再在上限时间内退出。
5. 再次启动不会复用旧令牌，也不会出现僵尸端口占用。

### M2：本地存储、迁移、备份与缓存

目标：先建立可靠数据底座，再开发业务功能。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M2-01 | 创建本地数据目录和权限检查 | M1 | 首次启动自动建目录；不可写或磁盘不足时阻止危险操作并提示 |
| M2-02 | 实现 core.db、cache.db 连接管理，启用 WAL、外键和忙等待策略 | M2-01 | 并发读写测试通过，SQLite 版本检查不低于基线 |
| M2-03 | 按技术设计创建核心表、索引和 FTS5 | M2-02 | 新数据库可从零迁移到最新版本，关系和唯一约束生效 |
| M2-04 | 实现前向迁移框架和启动前自动备份 | M2-03 | 从每个历史数据库样本升级成功；失败时原库仍可恢复 |
| M2-05 | 实现统一事务、仓储层和审计时间字段 | M2-03 | 关键多表写入要么全部成功，要么全部回滚 |
| M2-06 | 使用 Windows DPAPI 加密 API Key | M2-01 | 数据库和日志中看不到明文 Key；同一 Windows 用户可解密 |
| M2-07 | 实现 jobs 持久化、进度、取消和崩溃恢复状态 | M2-05 | 中断任务重启后标记为待恢复或失败，不永远显示“处理中” |
| M2-08 | 实现 cache.db 和缓存文件索引、LRU 清理与分类统计 | M2-02 | 达到约 450 MB 开始清理，任何时刻不超过 500 MB 硬上限 |
| M2-09 | 建立永久数据保护清单 | M2-08 | 清缓存不会删除 core.db、library/pdfs、模型、已保存译文、笔记或批注 |
| M2-10 | 实现手动备份、自动迁移备份和完整性检查 | M2-04 | 备份包含数据库和批注元数据，不包含 PDF；界面明确说明 |
| M2-11 | 实现 PDF 缺失记录与按哈希重新关联的底层接口 | M2-10 | 删除外部测试副本后能显示缺失状态，并用同内容文件重新关联 |
| M2-12 | 实现结构化日志、轮转和诊断包清单 | M2-01 | 日志有上限、可定位错误、无密钥和敏感全文 |

M2 数据耐久测试：

- 每次写入关键数据后立即结束进程，再启动并校验数据。
- 模拟磁盘满、数据库被占用、写入中断和迁移失败。
- 连续执行缓存压力测试，确认总量、清理顺序和永久数据保护。
- 从备份恢复到临时目录，核对文献、译文、收藏、笔记和批注数量。

### M3：三通道检索基础与文献合并

目标：完成可替换的数据源适配层，以及 WoS API、OpenAlex、Crossref 的检索和合并。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M3-01 | 定义统一查询、文献、作者、摘要、来源和错误数据契约 | M2 | 前后端和三个适配器使用同一领域模型 |
| M3-02 | 实现查询标准化、字段映射和分页游标 | M3-01 | 支持关键词、题名、主题、作者、DOI 精确查询和组合条件 |
| M3-03 | 实现 WoS Starter API 客户端 | M3-02 | API Key、超时、分页、限额、退避和可读错误均工作 |
| M3-04 | 实现 OpenAlex 客户端 | M3-02 | 可按 DOI/关键词补充元数据；正确还原 inverted index 摘要 |
| M3-05 | 实现 Crossref polite client | M3-02 | 带联系邮箱、合理频率限制，可按 DOI 补充出版信息 |
| M3-06 | 实现请求缓存、离线回退和“重新获取” | M3-03～M3-05 | 相同查询优先命中缓存；用户可明确刷新；离线显示缓存时间 |
| M3-07 | 实现 DOI、WoS UID、规范化题名等身份匹配 | M3-01 | 同一文献不因来源不同重复建档；冲突不会静默覆盖 |
| M3-08 | 实现字段级来源优先级、合并和 provenance | M3-07 | 详情页所需字段可追溯到来源和更新时间 |
| M3-09 | 实现速率限制、失败隔离和配额状态 | M3-03～M3-05 | 一个源故障不阻断其他源；免费额度耗尽时停止并提示 |
| M3-10 | 建立适配器契约测试和固定响应样本 | M3-03～M3-05 | 测试无需联网；API 字段变化能被测试发现 |
| M3-11 | 进行真实免费服务联调 | M3-10 | 使用少量请求核对真实结构、中文检索、摘要和限额响应 |

M3 验收场景：

- 用 DOI 查找一篇已知催化论文，多个来源合并为一条记录。
- 用英文关键词分页检索，保存后重启仍能查看记录和来源。
- WoS Key 缺失、无效、额度耗尽、网络断开时分别显示准确提示。
- OpenAlex 没有摘要或 Crossref 数据不全时，保留已获得字段，不制造内容。
- 同一文献字段冲突时，按规则选主值，并能看到其他来源值。

### M4：WoS 官方文件导入

目标：支持用户手动登录 WoS 后，把官方导出文件安全导入 App。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M4-01 | 实现上传格式、大小和内容签名校验 | M2 | 不只按扩展名判断；拒绝危险或不支持文件并说明原因 |
| M4-02 | 实现 WoS Plain Text 解析器 | M4-01 | 正确解析多记录及 AB、UT、DI 等核心字段 |
| M4-03 | 实现 RIS 解析器 | M4-01 | 作者、题名、期刊、年份、DOI、摘要正确映射 |
| M4-04 | 实现 Excel 解析器 | M4-01 | 兼容官方常见列名和空字段，限制公式和超大文件风险 |
| M4-05 | 实现导入预览、字段映射提示和错误行报告 | M4-02～M4-04 | 用户确认前不写核心库；可跳过错误记录 |
| M4-06 | 接入文献身份匹配和字段级来源合并 | M3-07、M3-08 | 导入记录能与 API 记录合并，保留“WoS 官方导入”来源 |
| M4-07 | 实现幂等导入和导入报告 | M4-06 | 同一文件重复导入不制造重复文献；显示新增、更新、跳过数 |
| M4-08 | 编写导入操作说明 | M4-07 | 明确指导从 WoS 导出 AB、UT、DI 及三种支持格式 |

### M5：核心界面、搜索结果与详情页

目标：把已经可用的后端能力组成完整的桌面浏览器主流程。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M5-01 | 建立微信风格但有差异化的设计令牌 | M1 | 催化青绿与铜金强调色、字号、间距、圆角、状态色统一 |
| M5-02 | 实现左侧导航和个人头像 | M5-01 | 包含首页、检索、期刊、知识库、设置；头像不显示“催”字 |
| M5-03 | 实现首次引导和设置入口 | M2、M5-02 | 可配置 API、翻译、存储位置展示、缓存和行为推荐开关 |
| M5-04 | 实现检索表单和高级条件 | M3 | 能创建、取消、翻页和重新执行查询，避免重复提交 |
| M5-05 | 实现检索进度与各来源状态 | M5-04 | 显示正在查询、成功、缓存、离线、限额和失败，不假装完整 |
| M5-06 | 实现结果列表、排序、筛选和批量操作 | M5-04 | 支持题名、作者、年份、期刊、来源、摘要可用性及状态筛选 |
| M5-07 | 实现文献详情页和来源面板 | M3-08 | 显示原文、译文区域、来源、冲突信息、知识库状态和 PDF 状态 |
| M5-08 | 实现从结果和详情加入知识库、喜欢和阅读状态 | M2、M5-06 | 三种状态含义清晰，刷新和重启后保留 |
| M5-09 | 接入 WoS 文件导入页面 | M4 | 可预览、确认并查看导入报告 |
| M5-10 | 实现空状态、加载状态、局部错误和恢复动作 | M5-03～M5-09 | 每个主要页面在无数据、离线、失败、限额下均有明确下一步 |
| M5-11 | 实现键盘可达、焦点、语义标签和基础响应式 | M5-02～M5-10 | 1366×768 及更高分辨率可用；主要操作可用键盘完成 |

M5 主流程验收：启动 → 搜索 → 查看来源状态 → 打开详情 → 加入知识库 → 修改阅读状态 → 重启 App → 数据仍在。

### M6：知识库、标签、笔记、状态与导出

目标：完成本地文献管理闭环。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M6-01 | 实现多知识库创建、改名、排序和删除 | M5 | 删除进入 30 天回收站，不立即物理清除文献和 PDF |
| M6-02 | 实现文献与知识库多对多关联 | M6-01 | 同一文献可进入多个知识库，但只有一份规范记录 |
| M6-03 | 实现标签创建、合并、删除和批量赋予 | M6-02 | 标签变更持久化，不造成悬空关系 |
| M6-04 | 实现普通笔记的自动保存、版本时间和失败提示 | M6-02 | 输入中断后可恢复；写入失败不得显示已保存 |
| M6-05 | 实现阅读状态、喜欢、收藏的独立模型 | M6-02 | 状态互不偷换，列表与详情同步 |
| M6-06 | 实现本地全文检索 | M2-03、M6-04 | 可检索题名、作者、摘要、译文、标签和普通笔记 |
| M6-07 | 实现回收站恢复和到期清理 | M6-01 | 30 天内可恢复；到期清理不误删仍被其他知识库引用的 PDF |
| M6-08 | 实现 BibTeX、RIS、CSV 导出 | M6-02 | 单条和批量导出；中文、特殊字符、换行和缺失字段正确 |
| M6-09 | 实现知识库统计和存储占用概览 | M6-02 | 区分数据库、原 PDF、模型和缓存占用 |

### M7：本地翻译

目标：使用已确认的 Hy-MT2-1.8B GGUF Q4_K_M，在本机按需翻译题名和摘要。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M7-01 | 实现模型清单、文件校验和缺失模型提示 | M0-05、M2 | 只接受已知来源和正确 SHA-256，校验失败不加载 |
| M7-02 | 实现 llama.cpp 服务按需启动、健康检查和互斥 | M1-09 | 第一次翻译才启动；并发请求不会重复启动多个模型进程 |
| M7-03 | 实现翻译队列、取消、超时、重试和进度 | M7-02、M2-07 | 长摘要可取消；失败保留原文并显示原因 |
| M7-04 | 实现题名、摘要分段和上下文拼接 | M7-03 | 不截断句子；保持段落；不翻译作者、DOI、公式和引用标记 |
| M7-05 | 实现催化领域术语表和用户术语表 | M7-04 | 用户可增删术语；术语保护可测试和关闭 |
| M7-06 | 实现译文缓存与“保存到知识库” | M7-04 | 未保存译文可被缓存清理；已保存译文永久保留 |
| M7-07 | 在详情页显示原文/译文对照、重译和状态 | M5-07、M7-06 | 原文永远保留，重译不静默覆盖已保存译文 |
| M7-08 | 实现无活动标签时模型退出 | M1-08、M7-02 | App 后台退出后无 llama.cpp 残留进程 |
| M7-09 | 在目标机测量质量、速度、内存和 CPU 占用 | M7-07 | 记录冷启动和典型摘要耗时；卡顿时限制并发而非让系统失控 |

说明：V1 翻译范围仅为题名和摘要。PDF 正文即使已经 OCR 或可提取，也不进入全文翻译。

### M8：PDF 入库、去重、解析与 OCR

目标：建立可靠的永久 PDF 管道，自动识别普通、扫描和混合页面。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M8-01 | 实现从知识库、结果页和详情页上传 PDF 的统一入口 | M6 | 三处使用同一后端管道和一致进度状态 |
| M8-02 | 实现流式上传、临时 .part 文件、大小限制和 PDF 内容校验 | M8-01 | 中断不产生永久脏文件；非 PDF、损坏和加密文件有明确提示 |
| M8-03 | 上传时计算 SHA-256 并采用内容寻址存储 | M8-02 | 写入成功后原子改名；同一二进制文件只保存一份 |
| M8-04 | 实现三级重复判断 | M8-03 | 依次判断精确哈希、同 DOI、规范题名+第一作者+年份弱匹配 |
| M8-05 | 实现重复处理确认界面 | M8-04 | 可关联已有 PDF、保留为新版本或取消；绝不静默覆盖 |
| M8-06 | 实现 paper_files 多关系和跨知识库单物理副本 | M8-03 | 同一 PDF 可关联多个文献/知识库，物理文件仍只有一份 |
| M8-07 | 用受限 PDFium 工作进程逐页检测文本密度和图像占比 | M8-03 | 每页标记普通、扫描或待复核，混合 PDF 不被整体误判 |
| M8-08 | 对普通页提取文本、布局、页码和字符位置 | M8-07 | 双栏和常见学术版式按可接受顺序提取，保留页级坐标 |
| M8-09 | 集成 PaddleOCR PP-OCRv6 处理扫描页 | M8-07 | 中英扫描样本可识别；OCR 只处理需要的页面，可取消 |
| M8-10 | 统一文本层与 OCR 层，保存页级来源和置信度 | M8-08、M8-09 | 阅读器知道每段来自原始文本还是 OCR，不混淆坐标 |
| M8-11 | 提取题名、摘要、DOI、作者和年份候选 | M8-10 | 每个候选有置信度和证据页；不确定时不自动覆盖 |
| M8-12 | 实现元数据核对和手动修正界面 | M8-11 | 用户可比较 PDF 候选与现有记录并逐字段确认 |
| M8-13 | 将题名和摘要接入本地翻译 | M7、M8-12 | 只翻译已确认或明确选择的候选，不翻译整篇正文 |
| M8-14 | 实现长任务限流、优先级、取消、重启恢复和临时文件清理 | M2-07、M8-09 | 同时只运行合理数量的重任务；崩溃后不会重复写库 |

PDF 处理验收：

- 同一文件改名上传：识别为精确重复，不增加物理副本。
- 同 DOI 的出版社版和作者版：提示版本重复，可保留两个版本。
- 无 DOI 且标题相似：只提示弱匹配，由用户决定。
- 混合 PDF：普通页不做 OCR，扫描页做 OCR。
- 无摘要、识别置信度低、加密或损坏：不制造内容，提供修正或终止路径。
- 上传后删除原下载位置：App 内仍可阅读，因为已复制到永久资料库。

### M9：PDF 阅读器与批注

目标：在 App 内完成阅读、定位和笔记化批注，不修改原 PDF。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M9-01 | 集成 PDF.js 并实现受控文件读取接口 | M8 | 浏览器不能通过任意路径读取本机文件，只能读已授权 PDF ID |
| M9-02 | 实现页码、缩放、适宽、适页、连续滚动和缩略图 | M9-01 | 普通、大页和旋转页面可正确显示 |
| M9-03 | 实现目录、页内文本选择和全文搜索 | M9-01、M8-10 | 有原文本层时直接用文本层；扫描页使用 OCR 文本层 |
| M9-04 | 保存和恢复最后阅读页、缩放和滚动位置 | M9-02 | 关闭和重启后回到合理位置；PDF 版本变化时安全回退 |
| M9-05 | 实现高亮 | M9-03 | 保存页码、规范化坐标、选中文字、上下文和文件哈希 |
| M9-06 | 实现划线 | M9-03 | 与高亮独立显示和管理，持久化规则一致 |
| M9-07 | 实现批注评论和编辑 | M9-05、M9-06 | 可对选区添加、编辑和删除评论；写入失败可恢复文本 |
| M9-08 | 将三类批注同步为知识笔记 | M6-04、M9-07 | 笔记可从文献和 PDF 双向打开到对应页和位置 |
| M9-09 | 实现批注列表、筛选和跳转 | M9-08 | 按页、类型和关键词查找并跳转 |
| M9-10 | 实现定位容错和失效标记 | M9-05 | 坐标、选中文字和上下文共同定位；PDF 换版本时不乱贴批注 |
| M9-11 | 保证 App-only 管理边界 | M9-07 | 下载或原文件字节不被修改；不提供批注写回和导出 |
| M9-12 | 做长文档性能和内存测试 | M9-02～M9-10 | 页面按需渲染，滚动后释放远端页面资源，不卡死浏览器 |

M9 验收场景：上传 PDF → 阅读到中间页 → 高亮一句 → 划线一句 → 写评论 → 关闭 App → 再次启动 → 回到上次位置 → 从知识笔记跳回原页。原 PDF SHA-256 在操作前后完全一致。

### M10：推荐、期刊关注、保存检索与提醒

目标：在 App 打开期间提供透明、可控、无后台常驻的更新和推荐能力。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M10-01 | 实现研究主题、关键词和催化领域偏好设置 | M5 | 可编辑、可关闭，不自动上传本地笔记或 PDF 内容 |
| M10-02 | 实现规则化推荐评分 | M6 | 基于主题、期刊、作者、年份、行为等已确认信号计算 |
| M10-03 | 生成每条推荐的可读原因 | M10-02 | 至少显示一个真实命中原因；无原因不展示为推荐 |
| M10-04 | 实现喜欢、收藏、阅读行为反馈开关 | M10-02 | 关闭行为推荐后不再使用历史行为，已有资料仍保留 |
| M10-05 | 实现期刊关注和取消关注 | M5 | 可查看最新检查时间和匹配数量 |
| M10-06 | 实现保存检索、手动运行和结果差异 | M5 | 能识别相对上次新增的文献，不重复提醒同一条 |
| M10-07 | 实现仅在 App 打开时运行的调度器 | M1-08、M10-05、M10-06 | 启动后按规则检查；后台退出即停止，不注册 Windows 常驻任务 |
| M10-08 | 实现 App 内提醒中心 | M10-07 | 显示新结果、失败和配额；不发送系统通知或邮件 |
| M10-09 | 实现推荐和更新请求的免费额度保护 | M3-09、M10-07 | 达到配额阈值停止自动检查，并显示下次可用信息 |

### M11：安全、性能、安装包与发布验收

目标：将开发版收敛为可长期使用、可升级、可诊断的 Windows 本地应用。

| 编号 | 任务 | 前置 | 产物与验收 |
|---|---|---|---|
| M11-01 | 完成威胁检查：回环服务、令牌、Origin、上传、路径和日志 | M1～M10 | 越权路径、目录穿越、伪造请求、恶意文件名和密钥泄漏测试通过 |
| M11-02 | 隔离 PDF/OCR/模型重任务并设置资源上限 | M7～M9 | 单任务崩溃不带崩主服务；CPU/内存并发有上限 |
| M11-03 | 对异常 PDF 和导入文件做负面与模糊测试 | M4、M8 | 损坏、超大、嵌套和异常编码输入不会造成任意文件访问或无限处理 |
| M11-04 | 做完整数据耐久和迁移演练 | M2～M10 | 从旧版本升级、异常断电模拟、备份恢复、PDF 缺失重联均通过 |
| M11-05 | 做 500 MB 缓存、PDF 大库和长时间运行压力测试 | M2、M8、M9 | 缓存不越界；永久 PDF 不被清理；无持续内存增长和僵尸进程 |
| M11-06 | 完成无网、弱网、限额和服务变更回归 | M3、M10 | 本地功能可用，外部失败均可解释和恢复 |
| M11-07 | 制作 Windows 安装包、桌面快捷方式和卸载流程 | M1、M0-08 | 安装不要求用户预装 Python/Node；快捷方式可启动 |
| M11-08 | 明确卸载时保留或删除本地资料的选择 | M11-07 | 默认不静默删除知识库和 PDF；危险选项二次确认 |
| M11-09 | 在纯净 Windows 测试机执行首次安装和升级安装 | M11-07 | 首装、配置、使用、退出、重启、升级均通过 |
| M11-10 | 完成第三方许可证、模型/OCR 声明和免费服务说明 | M0-05、M11-07 | 安装包内可查看，来源和许可证完整 |
| M11-11 | 编写用户说明、故障诊断和数据目录说明 | M11-09 | 非开发人员可完成密钥配置、导入、PDF 使用和数据迁移 |
| M11-12 | 执行发布候选全量回归并冻结版本 | M11-01～M11-11 | 无阻塞/严重缺陷，所有发布门槛签字通过 |

## 8. 阶段门禁

为避免页面看似完成但底层不可靠，设置五个强制门禁：

### Gate A：本地生命周期门禁（M1 后）

- 快捷方式启动、随机端口、单实例和浏览器打开均通过。
- 多标签心跳和约 2 分钟空闲退出通过。
- 正常、异常和强制退出均无残留子进程。

### Gate B：数据安全门禁（M2 后）

- 重启、崩溃和迁移不丢失核心数据。
- 密钥加密且不进入日志。
- 缓存压力测试不删除永久数据。
- 备份内容明确不含 PDF，恢复测试可用。

### Gate C：检索闭环门禁（M5 后）

- 三通道能独立工作、合并并显示来源。
- 断网、限额、Key 缺失和部分来源失败均有可恢复界面。
- 搜索、详情、入库和重启保持形成完整闭环。

### Gate D：阅读闭环门禁（M9 后）

- PDF 三级去重和普通/扫描/混合识别通过。
- PDF 永久保存、阅读、续读、搜索、批注和知识笔记互通。
- 原 PDF 哈希不变，批注不写回文件。

### Gate E：发布门禁（M11 后）

- 纯净机首装和升级安装通过。
- 全量自动化检查通过，无阻塞或严重缺陷。
- 用户数据、模型、缓存和 PDF 占用均可查看。
- 卸载不会在未确认时删除永久资料。

## 9. 验收测试矩阵

| 领域 | 必测内容 | 自动化层级 |
|---|---|---|
| 生命周期 | 单实例、多标签、刷新、睡眠、浏览器崩溃、空闲退出、子进程回收 | 集成 + E2E + 人工 |
| 数据库 | 新建、迁移、并发、事务回滚、异常中断、备份恢复 | 单元 + 集成 |
| API | 正常、空结果、分页、限额、超时、字段缺失、结构变化 | 契约 + 集成 |
| 文献合并 | DOI、WoS UID、题名弱匹配、字段冲突、来源追踪 | 单元 + 集成 |
| WoS 导入 | 三种格式、空字段、重复、错误记录、超大文件 | 单元 + E2E |
| 翻译 | 模型校验、启动、取消、术语、分段、保存、退出回收 | 单元 + 集成 + 人工质量抽检 |
| PDF 入库 | 中断上传、哈希重复、同 DOI、弱匹配、跨知识库复用 | 集成 + E2E |
| PDF 提取 | 普通、扫描、混合、双栏、中文、英文、损坏、加密 | 固定样本回归 + 人工抽检 |
| 阅读器 | 缩放、搜索、续读、原文本层、OCR 文本层、长文档 | 前端集成 + E2E |
| 批注 | 高亮、划线、评论、编辑、删除、跳转、版本失配 | 单元 + E2E |
| 缓存 | 450 MB 清理触发、500 MB 硬上限、永久数据保护 | 集成 + 压力 |
| 打包 | 首装、升级、卸载、快捷方式、无开发环境 | 纯净机冒烟 + 人工 |

测试数据必须使用公开、用户授权或自行构造的资料。真实学校登录凭据不得进入测试环境、截图、日志或版本库。

## 10. 关键风险与处理方式

| 风险 | 影响 | 预防与处理 |
|---|---|---|
| WoS 免费 API 额度或字段不足 | 主搜索结果不完整 | 强缓存、配额显示、OpenAlex/Crossref 补充、官方文件导入兜底 |
| 外部 API 结构变化 | 搜索失败或字段错位 | 适配器隔离、固定契约样本、字段级容错和可独立更新 |
| 本地模型在目标机过慢 | 翻译体验差 | M0 提前基准测试、按需加载、队列限并发、允许关闭翻译 |
| OCR 安装体积和速度较大 | 安装慢、处理占资源 | 模型清单校验、只 OCR 扫描页、任务限流、可取消和进度显示 |
| PDF 解析存在安全风险 | 本地服务崩溃或文件泄漏 | PDFium 受限工作进程、无 V8/XFA、大小/时间/内存限制、负面测试 |
| PDF 批注在版本变化后错位 | 笔记指向错误内容 | 同时保存文件哈希、页码、规范坐标、选中文字和上下文；失配则标记待重联 |
| PDF 不在 App 备份内 | 磁盘损坏后无法恢复原文 | 设置和备份页面持续明确提示；提供缺失检测和按哈希重联，不误导为完整备份 |
| PDF 永久资料增长不受缓存上限控制 | 磁盘空间不足 | 存储面板区分 PDF/模型/缓存，上传前检查空间，提供人工删除和引用检查 |
| SQLite 或原生依赖打包问题 | 安装版不可用 | M0 就做最小安装包；锁定 SQLite >= 3.51.3 和原生库版本 |
| 后台进程无法退出 | 违反只在 App 打开时运行 | 心跳状态机、Job Object、退出超时、僵尸进程自动化测试 |

## 11. 执行节奏和任务状态规则

每次只把一个里程碑中的少量任务置为进行中。建议工作循环如下：

1. 从未完成任务中选择所有依赖已满足的最小任务。
2. 在开始前写清本任务的输入、文件范围和验收命令。
3. 先补测试或固定失败样本，再实现功能。
4. 运行本任务检查和受影响的回归测试。
5. 更新任务状态、已知限制、实际结果和对应 ADR。
6. 达到里程碑退出条件后执行阶段门禁，不通过则不得宣布该阶段完成。

任务状态只使用：待开始、进行中、受阻、待验收、已完成。标记“受阻”必须写清缺少的输入以及不扩大范围的替代工作；标记“已完成”必须附自动化测试或人工验收证据。

每个开发批次交付说明至少包括：

- 完成的任务编号。
- 修改的主要文件和数据库迁移版本。
- 执行过的检查命令及结果。
- 需要人工验证的步骤。
- 仍存在的限制、风险和下一批任务。

## 12. PRD 覆盖关系

| PRD 能力 | 对应里程碑 |
|---|---|
| 单机、本地浏览器、快捷方式、按标签页生命周期退出 | M1、M11 |
| 本地持久化、SQLite、密钥、备份、缓存上限 | M2、M11 |
| WoS Starter API、OpenAlex、Crossref | M3 |
| WoS 手动登录后官方文件导入 | M4 |
| 首页、检索、期刊、知识库、设置、微信式差异化视觉、头像 | M5、M10 |
| 多知识库、收藏、喜欢、状态、标签、笔记、导出、回收站 | M6 |
| Hy-MT2 本地题名与摘要翻译 | M7 |
| PDF 永久保存、三级去重、普通/扫描/混合识别、OCR | M8 |
| PDF 阅读、高亮、划线、批注、笔记联动、App-only | M9 |
| 透明推荐、期刊关注、保存检索、App 内提醒 | M10 |
| 安全、性能、许可证、安装、升级和卸载 | M11 |

## 13. 第一批可以直接开始的任务

用户确认本计划后，第一批只执行 M0，不直接跳到页面编码：

1. M0-01：建立 PRD 需求追踪表。
2. M0-02：采集目标电脑的非敏感硬件基线。
3. M0-03：验证 SQLite、FTS5 和 WAL。
4. M0-04：验证 PDF、OCR 和翻译运行时兼容性。
5. M0-06：建立验收样本清单。
6. M0-08：完成最小 Windows 打包实验。

M0 通过后再创建正式工程骨架并进入 M1。任何阶段发现必须改变已确认需求时，应先向用户说明影响并更新 PRD；仅是实现方式变化时，更新技术设计和 ADR 即可。
