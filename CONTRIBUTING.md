# 贡献指南

这是一个以 Windows 单机、个人科研使用为边界的本地应用。提交变更前请先阅读 [PRD](PRD.md)、[技术设计](TECHNICAL_DESIGN.md)和[开发指南](docs/DEVELOPMENT.md)。功能建议若超出 V1 范围，应先更新需求文档并说明数据、安全和运行时影响。

## 开发流程

1. 从最新主分支创建短生命周期分支，例如 `feat/pdf-metadata-review` 或 `fix/idle-shutdown`。
2. 只提交源码、固定测试样本、配置和文档；不要提交依赖目录、构建包、模型、PDF 资料库、数据库、日志或密钥。
3. 功能和缺陷修复必须增加或更新有效测试。不得删除测试、跳过测试或降低断言来获得通过。
4. 数据库结构变化必须增加前向迁移和从上一版本升级的测试，不要求用户删除旧数据库。
5. 修改用户行为、数据边界或安全规则时，同步更新 PRD、技术设计、用户说明或 ADR 中适用的文件。
6. 提交前运行：

   ```powershell
   py -3.12 .\scripts\repository_hygiene.py
   powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1
   ```

## 提交信息

建议使用简洁的 Conventional Commits 风格：

- `feat:` 新功能；
- `fix:` 缺陷修复；
- `test:` 测试变更；
- `docs:` 纯文档变更；
- `refactor:` 不改变外部行为的重构；
- `build:` 构建、依赖或发布脚本变更。

一次提交应表达一个完整意图。不要把格式化、无关重命名和业务行为改变混在同一提交中。

## Pull Request 检查清单

- [ ] 变更仍符合“本人单机、本地优先、仅免费外部服务”的产品边界。
- [ ] 没有加入真实 API Key、学校 Cookie、私人 PDF、数据库、日志或用户笔记。
- [ ] 新增行为有自动化测试，失败路径和持久化路径已覆盖。
- [ ] Ruff、Mypy、前端检查、全部测试、生产构建和 E2E 均通过。
- [ ] 数据库和文件变更不会删除或静默覆盖用户永久资料。
- [ ] 相关文档、版本记录和第三方许可已经同步。

## 测试样本

`tests/fixtures/` 只能放入公开、明确授权、脱敏或程序生成的样本。不得提交未授权论文全文、学校账号信息、真实私人笔记或包含个人身份数据的导出文件。外部 API 响应应裁剪为覆盖契约所需的最小字段。
