# 安全政策

## 支持范围

当前维护版本为 `0.1.x`。项目仅面向 Windows 单机个人使用，不应部署到公网、局域网服务器或多人共享主机。

## 报告安全问题

不要在公开 Issue 中粘贴 API Key、学校登录信息、Cookie、PDF、摘要、笔记、数据库或日志原文。若仓库托管平台支持私密安全报告，请优先使用该渠道；否则先通过仓库所有者指定的私密联系方式报告最小复现信息，再按需提供脱敏材料。

报告建议包含：

- 受影响版本和 Windows 版本；
- 可复现步骤及预期/实际结果；
- 是否可能访问本机永久数据、密钥或回环服务；
- 已去除个人资料和密钥的诊断信息。

## 安全边界

- 后端、OCR 和翻译服务只能监听回环地址，不开放防火墙端口。
- 浏览器会话使用一次性启动令牌、HttpOnly Cookie、严格 Origin 校验和安全响应头。
- WoS/OpenAlex Key 使用当前 Windows 用户的 DPAPI 加密，不写入 Git、URL、浏览器存储或日志。
- PDF 在隔离工作进程中校验和解析，原文件按内容哈希保存，标注不写回 PDF。
- 模型与原生运行时只从固定官方来源获取并校验 SHA-256。
- App 不模拟学校登录，不绕过验证码、MFA、订阅、DRM 或付费墙。

请勿把本项目描述为云端备份或安全备份工具。内置备份不包含 PDF，磁盘或电脑损坏时仍可能丢失资料。
